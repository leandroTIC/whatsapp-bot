services:
  - name: whatsapp-bot
    type: web   # Pode ser web ou worker; worker não expõe porta
    env: docker
    plan: free
    autoDeploy: true
    source:
      type: git
      repositoryUrl: https://github.com/leandroTIC/whatsapp-bot.git
      branch: main
    buildCommand: ""      # Dockerfile cuida do build
    startCommand: ""      # Dockerfile cuida do start
